# DEV LOG - 2026-06-20

## Session Goal
- Move project to v0.2.6 and deliver two user-facing updates:
  - New Stylize Half-tone mode with size control.
  - Composite mask behavior that supports blurred Pen masks as feathered mattes (AE-like soft mask feel).

## Issues Reported During Session
- Composite mask with Pen -> Blur chain initially behaved as hard/full-frame mask instead of soft feathered matte.
- First live-mask fix iteration produced full reveal in some setups.
- Second iteration produced fully transparent output (showing nothing).

## Main Changes Applied

### 1) Version bump and v0.2.6 baseline
- Created v0.2.6 file from v0.2.5.
- Updated title/version metadata.

Files:
- oniwire_v0_2_6.html

### 2) UI cleanup placeholders removed
- Removed left panel placeholder note block.
- Removed inspector placeholder note block.
- Removed script fallback text that re-injected inspector placeholder copy when no node selected.

Files:
- oniwire_v0_2_6.html

### 3) Stylize Half-tone mode
- Added new Stylize mode: Half-tone.
- Added Half-tone size parameter (`halfToneSize`) with inspector slider.
- Implemented Half-tone rendering by combining grayscale/contrast stylization with dot-pattern overlay on top of source.

Files:
- scripts/nodes/stylize.js

### 4) Composite mask feather behavior for filtered masks
- Added filtered-mask detection in Composite (`hasStylizedMaskFilter`) to identify masks containing CSS filters (e.g., blur).
- Forced live mask path for filtered mask inputs so blur softness is preserved instead of flattening to hard vector/ramp mask logic.
- Added adaptive live mask update cadence:
  - 15 FPS for animated masks.
  - 5 FPS for static filtered masks.

Files:
- scripts/nodes/composite.js

### 5) Composite live-mask capture regression fixes
- Fixed case where mask showed full layer by ensuring live-mask capture path is used for filtered masks.
- Fixed case where output became empty by removing off-canvas transform from live mask driver clone used for capture.
- Kept live mask driver non-interactive and hidden from UI semantics while still renderable for capture.

Files:
- scripts/nodes/composite.js

### 6) Cache-busting for script updates in active v0.2.6 page
- Versioned Composite script include in v0.2.6 to ensure browser picked latest Composite node updates immediately.

Files:
- oniwire_v0_2_6.html

## Result Status at End of Session
- User confirmed success after final Composite live-mask fix.
- Pen -> Blur -> Composite mask now behaves as intended with feathered/soft matte response.
- Half-tone Stylize mode is available with user-adjustable size.

## Notes
- This session included iterative mask pipeline debugging across multiple failure modes (full reveal -> no reveal) before converging on stable live capture behavior.
- If future demand increases, consider exposing explicit Composite mask mode controls (Alpha/Luma/Invert variants) in inspector for predictable matte workflows.
