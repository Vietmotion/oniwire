# DEV LOG — 2026-03-22

## What changed
- Replaced the old MediaRecorder-based MP4 export in `index.html` and `oniwire_v0_2_2.html` with a frame-accurate path using WebCodecs + `mp4-muxer`.
- MP4 export now renders each frame with an off-screen `html2canvas` clone of `renderRoot`, then assigns explicit per-frame timestamps during encoding.
- Updated animation freezing logic so CSS animations use paused negative delays while Web Animations API instances are advanced separately.
- Added MP4 progress phase text styling in `styles.css`.

## Bugs addressed
- Low-resolution MP4 output caused by scaling captured frames up during encoding.
- Incorrect exported duration caused by MediaRecorder using wall-clock timing instead of intended animation timing.
- Motion nodes appearing static in export because the old capture path relied on `renderLayerToDataUrl(renderRoot)`, which did not preserve animated computed styles correctly.

## Repair work
- Fixed a bad patch merge that duplicated the MP4 block inside `finalizeSelection()`.
- Restored the editor `mousemove` handler in both HTML files.
- Removed stray leftover code fragments after the handler restoration.
- Verified no editor diagnostics remain in `index.html`, `oniwire_v0_2_2.html`, or `styles.css`.

## Browser test results (end of day)
- ✅ Duration correct — 5s animation now exports as ~5s MP4 (not 12s wall-clock).
- ✅ Motion nodes animate — circles visibly rotate in the exported video.
- ❌ Text mask broken — text-masked layers render with an error in the export. Root cause unknown; needs investigation tomorrow.

## Pending for next session
- Debug why text mask fails during `html2canvas` frame capture.
  - Likely candidates: `clip-path` not captured by html2canvas, or the off-screen clone loses mask layer references.
  - Check whether the mask element is appended to the clone correctly, or if SVG `clipPath` / CSS `mask` is involved.
- Confirm fix applies equally to `oniwire_v0_2_2.html`.