- Date: 2026-03-13
- Completed: Added a new Lottie preview section to Test page with three cards below the existing Oniwire hero demo.
- Completed: Wired two JSON assets and one `.lottie` asset from `example-lottie-json` using LottieFiles web components.
- Completed: Matched the new section to the existing glass-panel visual style and made the layout responsive.
- Verified: HTML diagnostics report no errors in `Test folder/Test page.html` after the update.
- Pending: If local `file://` loading blocks animation fetches in the browser, serve the workspace over HTTP for reliable asset loading.
- Completed: Added separate Output export action `Lottie JSON` (Beta) without changing existing Embed JS export behavior.
- Completed: Added first-pass Lottie serializer in `scripts/export.js` for supported nodes: `Shape`, `Transform`, `Motion (breath)`, and `Color`.
- Completed: Added compatibility guard that blocks Lottie export and shows unsupported node types when graph uses unsupported nodes (e.g. `Clone`, `Glow`, `Mask`, `Composite`).
- Completed: Added Output inspector checkboxes for export mode: `Embed JS` and `Lottie JSON (block unsupported nodes)`.
- Completed: Made export-mode checkboxes mutually exclusive and default-safe (`Embed JS` remains enabled if both are unticked).
- Completed: When Lottie mode is checked, node add/paste now blocks unsupported node types and shows a toast reason.
- Completed: Node dropdown now visually disables blocked node types while Lottie mode is active.
- Completed: Fixed node-search feedback so blocked node creation in Lottie mode no longer shows `Added`; it now shows a blocked message.
- Completed: Increased toast visibility duration from 1.5s to 3.2s so warnings and mode-block messages are readable.
- Completed: Added beta Lottie export support for `Text` and updated Lottie mode allowlist to include `Text` in editor node-blocking rules.
- Completed: Reworked Text export for compatibility by rasterizing text into embedded PNG image assets (`ty:2`) instead of fragile Lottie text layers (`ty:5`).
- Pending: Validate Text -> Motion -> Output export in browser and confirm the generated file renders in the test page player.
- Completed: Extracted `helloboize.png` from `helloboize.json` and confirmed the JSON image-asset variant still does not render in the current test-page player path.
- Completed: Switched the middle preview card on `Test folder/Test page.html` to a direct animated PNG fallback so the exported result is visible even when the player skips image-backed Lottie.
- Completed: Refactored `scripts/export.js` Lottie serializer to emit layer-local shape geometry, tighter text image assets, and eased Bodymovin-style scale keyframes so exported motion timing and pivot behavior match Oniwire more closely.
- Completed: Added `Composite` support in Lottie export for foreground-over-background stacking with blend + opacity, and unblocked `Composite` in Lottie mode add/paste restrictions.
- Pending: Rework Oniwire Lottie text-image export packaging so browser preview does not need a PNG fallback.