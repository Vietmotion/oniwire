# DEV LOG - 2026-03-26

## Deployment workflow note
- GitHub live app is served from main/index.html.
- Always sync newest version file (oniwire_vX_Y_Z.html) into index.html before commit/push.
- Current helper script: scripts/sync-index.ps1 (copies latest semantic version file to index.html).
- Commit both updated version file and index.html for live behavior changes.

## Why this note
- Prevents pushing a newer version file while live site still serves an older index.html.

## Motion + Composite Mask issue (paused)
- Issue: when a moving shape is used as Composite/Mask input, the masked result can disappear (blank preview).
- Current status: unresolved; paused for later focused debugging.

### Attempts made today
- Treated motion masks as live masks in both mask and composite nodes.
- Added live mask driver elements for animated mask sampling.
- Updated live mask refresh path to capture mask frames continuously.
- Switched live mask mode to alpha and enabled transparent capture mode for mask snapshots.

### Observation
- Blank result still occurs in some motion + composite-mask graphs.

### Next debugging plan
- Add a temporary debug view toggle to preview the raw live mask texture.
- Compare mask frame generation vs. mask application stage to isolate where pixels are lost.
- Validate mask frame dimensions and alpha content before assignment to maskImage.
