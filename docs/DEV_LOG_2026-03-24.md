# DEV LOG — 2026-03-24

## What changed
- Added `ColorCorrect` to the node menu list and mapped display label to `Color Corrector`.
- Updated UI naming so `ColorCorrect` shows as `Color Corrector` across node card title, inspector name fallback, node search list, and help navigation.
- Extended Text node font list to a curated top 30 set in `scripts/nodes/text.js`.
- Added Google Fonts loading links in `index.html` and `oniwire_v0_2_2.html` so non-system fonts (e.g., Mulish, Manrope, Oswald) render correctly.
- Upgraded Shape node from single `size` to `width` + `height`, renamed `square` option to `rectangle`, and added `uniform` toggle in inspector.
- Synced uniform behavior in inspector handlers so editing width/height keeps both values locked when enabled.

## New feature: Curves node
- Created `scripts/nodes/curves.js` with channel-based curve controls and SVG filter application via `feComponentTransfer`.
- Added Curves node registration in `scripts/nodes.js`.
- Added Curves to menu under Effect in `scripts/node-menu.js`.
- Added Curves script includes, tips content, and category mapping in:
  - `index.html`
  - `oniwire_v0_2_2.html`
  - `oniwire_v0_2_1.html`

## Curves inspector UX upgrade
- Implemented draggable curve graph UI (Photoshop-style interaction) for Curves mode controls (`RGB`, `R`, `G`, `B`).
- Added live interactive handles for Shadows / Midtones / Highlights and a Reset button.
- Bound drag interactions to existing numeric/range params so graph + controls stay synchronized.
- Applied this inspector UI behavior in all three entry pages:
  - `index.html`
  - `oniwire_v0_2_2.html`
  - `oniwire_v0_2_1.html`

## Bugs addressed
- Fixed missing ColorCorrect visibility in node tab dropdown (node existed but was not included in menu group list).
- Fixed non-loading fonts by actually importing Google-hosted families.
- Fixed stretched curve handles (oval dots) by removing non-uniform SVG scaling and switching to dynamic viewBox based on rendered size.

## Validation status
- Ran editor diagnostics after each patch set.
- No errors reported in modified files at end of session.

## Pending for next session
- Curves enhancement: support more than 3 control points with true smooth spline interpolation (closer to full Photoshop curves workflow).
- Curves UX polish: add axis labels (`Input`/`Output`) and optional numeric readout tooltip while dragging.
- Consider replacing current linear segment interpolation in the filter table with smoother cubic interpolation for finer tonal transitions.
